---
description: Fix performance issues like slow queries, unnecessary re-renders, bundle bloat, or inefficient algorithms
---

Follow the instructions in `.agents/skills/performance/SKILL.md`.

Windsurf-specific additions:
// turbo
- Run `bun run lint`.
// turbo
- Run `bun run build`.
