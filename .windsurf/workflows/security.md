---
description: Fix security vulnerabilities like weak validation, secret exposure, injection risks, or auth bypass
---

Follow the instructions in `.agents/skills/security/SKILL.md`.

Windsurf-specific additions:
// turbo
- Run `bun run lint`.
// turbo
- Run `bun run build`.
