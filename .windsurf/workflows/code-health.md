---
description: Fix code health issues like duplication, dead code, naming, deprecated usage, or complexity
---

Follow the instructions in `.agents/skills/code-health/SKILL.md`.

Windsurf-specific additions:
// turbo
- Run `bun run lint`.
// turbo
- Run `bun run build`.
